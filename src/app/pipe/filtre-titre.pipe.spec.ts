import { FiltreTitrePipe } from './filtre-titre.pipe';

describe('FiltreTitrePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreTitrePipe();
    expect(pipe).toBeTruthy();
  });
});
