import { FiltreValuePipe } from './filtre-value.pipe';

describe('FiltreValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreValuePipe();
    expect(pipe).toBeTruthy();
  });
});
